import { data } from './data';
import { reset, getState, addPlayer, addWeapons, play, checkIfWinner } from './use-cases';

export {
  data,
  reset,
  getState,
  addPlayer,
  addWeapons,
  play,
  checkIfWinner
};
